// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/valocode/bubbly/ent/release"
	"github.com/valocode/bubbly/ent/releasecheck"
)

// ReleaseCheck is the model entity for the ReleaseCheck schema.
type ReleaseCheck struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Type holds the value of the "type" field.
	Type releasecheck.Type `json:"type,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ReleaseCheckQuery when eager-loading is set.
	Edges                 ReleaseCheckEdges `json:"edges"`
	release_check_release *int
}

// ReleaseCheckEdges holds the relations/edges for other nodes in the graph.
type ReleaseCheckEdges struct {
	// Release holds the value of the release edge.
	Release *Release `json:"release,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ReleaseOrErr returns the Release value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ReleaseCheckEdges) ReleaseOrErr() (*Release, error) {
	if e.loadedTypes[0] {
		if e.Release == nil {
			// The edge release was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: release.Label}
		}
		return e.Release, nil
	}
	return nil, &NotLoadedError{edge: "release"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ReleaseCheck) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case releasecheck.FieldID:
			values[i] = new(sql.NullInt64)
		case releasecheck.FieldType:
			values[i] = new(sql.NullString)
		case releasecheck.ForeignKeys[0]: // release_check_release
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type ReleaseCheck", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ReleaseCheck fields.
func (rc *ReleaseCheck) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case releasecheck.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			rc.ID = int(value.Int64)
		case releasecheck.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				rc.Type = releasecheck.Type(value.String)
			}
		case releasecheck.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field release_check_release", value)
			} else if value.Valid {
				rc.release_check_release = new(int)
				*rc.release_check_release = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryRelease queries the "release" edge of the ReleaseCheck entity.
func (rc *ReleaseCheck) QueryRelease() *ReleaseQuery {
	return (&ReleaseCheckClient{config: rc.config}).QueryRelease(rc)
}

// Update returns a builder for updating this ReleaseCheck.
// Note that you need to call ReleaseCheck.Unwrap() before calling this method if this ReleaseCheck
// was returned from a transaction, and the transaction was committed or rolled back.
func (rc *ReleaseCheck) Update() *ReleaseCheckUpdateOne {
	return (&ReleaseCheckClient{config: rc.config}).UpdateOne(rc)
}

// Unwrap unwraps the ReleaseCheck entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rc *ReleaseCheck) Unwrap() *ReleaseCheck {
	tx, ok := rc.config.driver.(*txDriver)
	if !ok {
		panic("ent: ReleaseCheck is not a transactional entity")
	}
	rc.config.driver = tx.drv
	return rc
}

// String implements the fmt.Stringer.
func (rc *ReleaseCheck) String() string {
	var builder strings.Builder
	builder.WriteString("ReleaseCheck(")
	builder.WriteString(fmt.Sprintf("id=%v", rc.ID))
	builder.WriteString(", type=")
	builder.WriteString(fmt.Sprintf("%v", rc.Type))
	builder.WriteByte(')')
	return builder.String()
}

// ReleaseChecks is a parsable slice of ReleaseCheck.
type ReleaseChecks []*ReleaseCheck

func (rc ReleaseChecks) config(cfg config) {
	for _i := range rc {
		rc[_i].config = cfg
	}
}
