// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/valocode/bubbly/ent/licensescan"
	"github.com/valocode/bubbly/ent/release"
	"github.com/valocode/bubbly/ent/releaseentry"
)

// LicenseScan is the model entity for the LicenseScan schema.
type LicenseScan struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Tool holds the value of the "tool" field.
	Tool string `json:"tool,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the LicenseScanQuery when eager-loading is set.
	Edges                      LicenseScanEdges `json:"edges"`
	license_scan_release       *int
	release_entry_license_scan *int
}

// LicenseScanEdges holds the relations/edges for other nodes in the graph.
type LicenseScanEdges struct {
	// Release holds the value of the release edge.
	Release *Release `json:"release,omitempty"`
	// Entry holds the value of the entry edge.
	Entry *ReleaseEntry `json:"entry,omitempty"`
	// Licenses holds the value of the licenses edge.
	Licenses []*LicenseUsage `json:"licenses,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// ReleaseOrErr returns the Release value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LicenseScanEdges) ReleaseOrErr() (*Release, error) {
	if e.loadedTypes[0] {
		if e.Release == nil {
			// The edge release was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: release.Label}
		}
		return e.Release, nil
	}
	return nil, &NotLoadedError{edge: "release"}
}

// EntryOrErr returns the Entry value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LicenseScanEdges) EntryOrErr() (*ReleaseEntry, error) {
	if e.loadedTypes[1] {
		if e.Entry == nil {
			// The edge entry was loaded in eager-loading,
			// but was not found.
			return nil, &NotFoundError{label: releaseentry.Label}
		}
		return e.Entry, nil
	}
	return nil, &NotLoadedError{edge: "entry"}
}

// LicensesOrErr returns the Licenses value or an error if the edge
// was not loaded in eager-loading.
func (e LicenseScanEdges) LicensesOrErr() ([]*LicenseUsage, error) {
	if e.loadedTypes[2] {
		return e.Licenses, nil
	}
	return nil, &NotLoadedError{edge: "licenses"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LicenseScan) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case licensescan.FieldID:
			values[i] = new(sql.NullInt64)
		case licensescan.FieldTool:
			values[i] = new(sql.NullString)
		case licensescan.ForeignKeys[0]: // license_scan_release
			values[i] = new(sql.NullInt64)
		case licensescan.ForeignKeys[1]: // release_entry_license_scan
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type LicenseScan", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LicenseScan fields.
func (ls *LicenseScan) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case licensescan.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ls.ID = int(value.Int64)
		case licensescan.FieldTool:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tool", values[i])
			} else if value.Valid {
				ls.Tool = value.String
			}
		case licensescan.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field license_scan_release", value)
			} else if value.Valid {
				ls.license_scan_release = new(int)
				*ls.license_scan_release = int(value.Int64)
			}
		case licensescan.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field release_entry_license_scan", value)
			} else if value.Valid {
				ls.release_entry_license_scan = new(int)
				*ls.release_entry_license_scan = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryRelease queries the "release" edge of the LicenseScan entity.
func (ls *LicenseScan) QueryRelease() *ReleaseQuery {
	return (&LicenseScanClient{config: ls.config}).QueryRelease(ls)
}

// QueryEntry queries the "entry" edge of the LicenseScan entity.
func (ls *LicenseScan) QueryEntry() *ReleaseEntryQuery {
	return (&LicenseScanClient{config: ls.config}).QueryEntry(ls)
}

// QueryLicenses queries the "licenses" edge of the LicenseScan entity.
func (ls *LicenseScan) QueryLicenses() *LicenseUsageQuery {
	return (&LicenseScanClient{config: ls.config}).QueryLicenses(ls)
}

// Update returns a builder for updating this LicenseScan.
// Note that you need to call LicenseScan.Unwrap() before calling this method if this LicenseScan
// was returned from a transaction, and the transaction was committed or rolled back.
func (ls *LicenseScan) Update() *LicenseScanUpdateOne {
	return (&LicenseScanClient{config: ls.config}).UpdateOne(ls)
}

// Unwrap unwraps the LicenseScan entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ls *LicenseScan) Unwrap() *LicenseScan {
	tx, ok := ls.config.driver.(*txDriver)
	if !ok {
		panic("ent: LicenseScan is not a transactional entity")
	}
	ls.config.driver = tx.drv
	return ls
}

// String implements the fmt.Stringer.
func (ls *LicenseScan) String() string {
	var builder strings.Builder
	builder.WriteString("LicenseScan(")
	builder.WriteString(fmt.Sprintf("id=%v", ls.ID))
	builder.WriteString(", tool=")
	builder.WriteString(ls.Tool)
	builder.WriteByte(')')
	return builder.String()
}

// LicenseScans is a parsable slice of LicenseScan.
type LicenseScans []*LicenseScan

func (ls LicenseScans) config(cfg config) {
	for _i := range ls {
		ls[_i].config = cfg
	}
}
