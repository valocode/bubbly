// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/valocode/bubbly/ent/license"
	"github.com/valocode/bubbly/ent/licensescan"
	"github.com/valocode/bubbly/ent/licenseusage"
)

// LicenseUsageCreate is the builder for creating a LicenseUsage entity.
type LicenseUsageCreate struct {
	config
	mutation *LicenseUsageMutation
	hooks    []Hook
}

// SetLicenseID sets the "license" edge to the License entity by ID.
func (luc *LicenseUsageCreate) SetLicenseID(id int) *LicenseUsageCreate {
	luc.mutation.SetLicenseID(id)
	return luc
}

// SetLicense sets the "license" edge to the License entity.
func (luc *LicenseUsageCreate) SetLicense(l *License) *LicenseUsageCreate {
	return luc.SetLicenseID(l.ID)
}

// SetScanID sets the "scan" edge to the LicenseScan entity by ID.
func (luc *LicenseUsageCreate) SetScanID(id int) *LicenseUsageCreate {
	luc.mutation.SetScanID(id)
	return luc
}

// SetScan sets the "scan" edge to the LicenseScan entity.
func (luc *LicenseUsageCreate) SetScan(l *LicenseScan) *LicenseUsageCreate {
	return luc.SetScanID(l.ID)
}

// Mutation returns the LicenseUsageMutation object of the builder.
func (luc *LicenseUsageCreate) Mutation() *LicenseUsageMutation {
	return luc.mutation
}

// Save creates the LicenseUsage in the database.
func (luc *LicenseUsageCreate) Save(ctx context.Context) (*LicenseUsage, error) {
	var (
		err  error
		node *LicenseUsage
	)
	if len(luc.hooks) == 0 {
		if err = luc.check(); err != nil {
			return nil, err
		}
		node, err = luc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*LicenseUsageMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = luc.check(); err != nil {
				return nil, err
			}
			luc.mutation = mutation
			if node, err = luc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(luc.hooks) - 1; i >= 0; i-- {
			mut = luc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, luc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (luc *LicenseUsageCreate) SaveX(ctx context.Context) *LicenseUsage {
	v, err := luc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (luc *LicenseUsageCreate) check() error {
	if _, ok := luc.mutation.LicenseID(); !ok {
		return &ValidationError{Name: "license", err: errors.New("ent: missing required edge \"license\"")}
	}
	if _, ok := luc.mutation.ScanID(); !ok {
		return &ValidationError{Name: "scan", err: errors.New("ent: missing required edge \"scan\"")}
	}
	return nil
}

func (luc *LicenseUsageCreate) sqlSave(ctx context.Context) (*LicenseUsage, error) {
	_node, _spec := luc.createSpec()
	if err := sqlgraph.CreateNode(ctx, luc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (luc *LicenseUsageCreate) createSpec() (*LicenseUsage, *sqlgraph.CreateSpec) {
	var (
		_node = &LicenseUsage{config: luc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: licenseusage.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: licenseusage.FieldID,
			},
		}
	)
	if nodes := luc.mutation.LicenseIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   licenseusage.LicenseTable,
			Columns: []string{licenseusage.LicenseColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: license.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.license_usage_license = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := luc.mutation.ScanIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   licenseusage.ScanTable,
			Columns: []string{licenseusage.ScanColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: licensescan.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.license_usage_scan = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// LicenseUsageCreateBulk is the builder for creating many LicenseUsage entities in bulk.
type LicenseUsageCreateBulk struct {
	config
	builders []*LicenseUsageCreate
}

// Save creates the LicenseUsage entities in the database.
func (lucb *LicenseUsageCreateBulk) Save(ctx context.Context) ([]*LicenseUsage, error) {
	specs := make([]*sqlgraph.CreateSpec, len(lucb.builders))
	nodes := make([]*LicenseUsage, len(lucb.builders))
	mutators := make([]Mutator, len(lucb.builders))
	for i := range lucb.builders {
		func(i int, root context.Context) {
			builder := lucb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*LicenseUsageMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, lucb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, lucb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, lucb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (lucb *LicenseUsageCreateBulk) SaveX(ctx context.Context) []*LicenseUsage {
	v, err := lucb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
