// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/valocode/bubbly/ent/licensescan"
	"github.com/valocode/bubbly/ent/licenseusage"
	"github.com/valocode/bubbly/ent/release"
	"github.com/valocode/bubbly/ent/releaseentry"
)

// LicenseScanCreate is the builder for creating a LicenseScan entity.
type LicenseScanCreate struct {
	config
	mutation *LicenseScanMutation
	hooks    []Hook
}

// SetTool sets the "tool" field.
func (lsc *LicenseScanCreate) SetTool(s string) *LicenseScanCreate {
	lsc.mutation.SetTool(s)
	return lsc
}

// SetReleaseID sets the "release" edge to the Release entity by ID.
func (lsc *LicenseScanCreate) SetReleaseID(id int) *LicenseScanCreate {
	lsc.mutation.SetReleaseID(id)
	return lsc
}

// SetRelease sets the "release" edge to the Release entity.
func (lsc *LicenseScanCreate) SetRelease(r *Release) *LicenseScanCreate {
	return lsc.SetReleaseID(r.ID)
}

// SetEntryID sets the "entry" edge to the ReleaseEntry entity by ID.
func (lsc *LicenseScanCreate) SetEntryID(id int) *LicenseScanCreate {
	lsc.mutation.SetEntryID(id)
	return lsc
}

// SetNillableEntryID sets the "entry" edge to the ReleaseEntry entity by ID if the given value is not nil.
func (lsc *LicenseScanCreate) SetNillableEntryID(id *int) *LicenseScanCreate {
	if id != nil {
		lsc = lsc.SetEntryID(*id)
	}
	return lsc
}

// SetEntry sets the "entry" edge to the ReleaseEntry entity.
func (lsc *LicenseScanCreate) SetEntry(r *ReleaseEntry) *LicenseScanCreate {
	return lsc.SetEntryID(r.ID)
}

// AddLicenseIDs adds the "licenses" edge to the LicenseUsage entity by IDs.
func (lsc *LicenseScanCreate) AddLicenseIDs(ids ...int) *LicenseScanCreate {
	lsc.mutation.AddLicenseIDs(ids...)
	return lsc
}

// AddLicenses adds the "licenses" edges to the LicenseUsage entity.
func (lsc *LicenseScanCreate) AddLicenses(l ...*LicenseUsage) *LicenseScanCreate {
	ids := make([]int, len(l))
	for i := range l {
		ids[i] = l[i].ID
	}
	return lsc.AddLicenseIDs(ids...)
}

// Mutation returns the LicenseScanMutation object of the builder.
func (lsc *LicenseScanCreate) Mutation() *LicenseScanMutation {
	return lsc.mutation
}

// Save creates the LicenseScan in the database.
func (lsc *LicenseScanCreate) Save(ctx context.Context) (*LicenseScan, error) {
	var (
		err  error
		node *LicenseScan
	)
	if len(lsc.hooks) == 0 {
		if err = lsc.check(); err != nil {
			return nil, err
		}
		node, err = lsc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*LicenseScanMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = lsc.check(); err != nil {
				return nil, err
			}
			lsc.mutation = mutation
			if node, err = lsc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(lsc.hooks) - 1; i >= 0; i-- {
			mut = lsc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, lsc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (lsc *LicenseScanCreate) SaveX(ctx context.Context) *LicenseScan {
	v, err := lsc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// check runs all checks and user-defined validators on the builder.
func (lsc *LicenseScanCreate) check() error {
	if _, ok := lsc.mutation.Tool(); !ok {
		return &ValidationError{Name: "tool", err: errors.New("ent: missing required field \"tool\"")}
	}
	if v, ok := lsc.mutation.Tool(); ok {
		if err := licensescan.ToolValidator(v); err != nil {
			return &ValidationError{Name: "tool", err: fmt.Errorf("ent: validator failed for field \"tool\": %w", err)}
		}
	}
	if _, ok := lsc.mutation.ReleaseID(); !ok {
		return &ValidationError{Name: "release", err: errors.New("ent: missing required edge \"release\"")}
	}
	return nil
}

func (lsc *LicenseScanCreate) sqlSave(ctx context.Context) (*LicenseScan, error) {
	_node, _spec := lsc.createSpec()
	if err := sqlgraph.CreateNode(ctx, lsc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (lsc *LicenseScanCreate) createSpec() (*LicenseScan, *sqlgraph.CreateSpec) {
	var (
		_node = &LicenseScan{config: lsc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: licensescan.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: licensescan.FieldID,
			},
		}
	)
	if value, ok := lsc.mutation.Tool(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: licensescan.FieldTool,
		})
		_node.Tool = value
	}
	if nodes := lsc.mutation.ReleaseIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   licensescan.ReleaseTable,
			Columns: []string{licensescan.ReleaseColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: release.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.license_scan_release = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := lsc.mutation.EntryIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2O,
			Inverse: true,
			Table:   licensescan.EntryTable,
			Columns: []string{licensescan.EntryColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: releaseentry.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.release_entry_license_scan = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := lsc.mutation.LicensesIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: true,
			Table:   licensescan.LicensesTable,
			Columns: []string{licensescan.LicensesColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: licenseusage.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// LicenseScanCreateBulk is the builder for creating many LicenseScan entities in bulk.
type LicenseScanCreateBulk struct {
	config
	builders []*LicenseScanCreate
}

// Save creates the LicenseScan entities in the database.
func (lscb *LicenseScanCreateBulk) Save(ctx context.Context) ([]*LicenseScan, error) {
	specs := make([]*sqlgraph.CreateSpec, len(lscb.builders))
	nodes := make([]*LicenseScan, len(lscb.builders))
	mutators := make([]Mutator, len(lscb.builders))
	for i := range lscb.builders {
		func(i int, root context.Context) {
			builder := lscb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*LicenseScanMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, lscb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, lscb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, lscb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (lscb *LicenseScanCreateBulk) SaveX(ctx context.Context) []*LicenseScan {
	v, err := lscb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
